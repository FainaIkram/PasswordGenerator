# PasswordGenerator
A simple Python-based Random Password Generator that creates secure passwords using random combinations of letters and numbers. This project demonstrates the use of Python's built-in `random` and `string` modules, user input handling, loops, and string manipulation to generate customizable passwords of any desired length.
